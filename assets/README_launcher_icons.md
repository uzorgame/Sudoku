# Launcher icon assets

Add the following files before running `flutter pub run flutter_launcher_icons`:

- `assets/icon.png`: 1024×1024 PNG with background.
- `assets/icon_foreground.png`: 1024×1024 PNG with transparent background.

These files are referenced from `pubspec.yaml` and will be used to regenerate the Android and iOS launcher icons.
